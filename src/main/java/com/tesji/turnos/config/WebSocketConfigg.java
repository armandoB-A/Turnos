package com.tesji.turnos.config;

public class WebSocketConfigg {
/*
    @Autowired
    private TurnoRepository turnoRepository;

    @Override
    public void registerWebSocketHandlers(WebSocketHandlerRegistry registry) {
        registry.addHandler(itemWebSocketHandler(), "/itemWebSocketHandler").setAllowedOrigins("*");
    }

    @Bean
    public WebSocketHandler itemWebSocketHandler() {
        return new TableWebSocketHandler(turnoRepository);
    }
 */

}